Options -Indexes

# Serve index.php before index.html
DirectoryIndex index.php index.html

# PHP error logging
php_flag display_errors Off
php_flag log_errors On

# Charset padrão
AddDefaultCharset UTF-8

# ── URLs amigáveis ────────────────────────────────────────────────────────────
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Não reescrever arquivos ou diretórios que existem
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Páginas principais (pasta paginas/)
    RewriteRule ^treinamento/?$    paginas/treinamento.php    [L]
    RewriteRule ^desenvolvimento/?$ paginas/desenvolvimento.php [L]
    RewriteRule ^consultoria/?$    paginas/consultoria.php    [L]
    RewriteRule ^servicos/?$       paginas/servicos.php       [L]
    RewriteRule ^blog/?$           paginas/blog.php           [L]
    RewriteRule ^guideline/?$      paginas/guideline.php      [L]

    # Posts do blog: /blog/meu-post → post.php?slug=meu-post
    RewriteRule ^blog/([a-z0-9\-]+)/?$ paginas/post.php?slug=$1 [L,QSA]
</IfModule>

# ── Segurança ─────────────────────────────────────────────────────────────────
<FilesMatch "^config\.php$">
    Order Allow,Deny
    Deny from all
</FilesMatch>
